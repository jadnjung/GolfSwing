# local-database

Typed repository layer over the on-device SQLite database (PRD section 7.5). Structured metadata only — large binaries (video, pose data) live on the filesystem, not in the database. Not yet implemented.
