# packages

Shared TypeScript packages consumed by `apps/mobile` and each other. Only `tooling-smoke-test` currently contains real code — it exists to prove the workspace, TypeScript, lint, and test tooling actually work. The other packages below are architectural placeholders (per PRD section 7.2 / section 7.1 domain layer) that will be filled in as the corresponding features are built.
