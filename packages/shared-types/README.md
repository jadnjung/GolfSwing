# shared-types

TypeScript types/interfaces shared across packages and the app (e.g. data-model shapes from PRD section 7.7) where they need to be referenced without depending on `domain`'s implementation. Not yet implemented.
