# apps

Deployable applications. `apps/mobile` (the React Native app) is created during app scaffolding — not yet present. See `docs/adr/0002-toolchain-baseline.md` for the target React Native version.
