# models

On-device ML model assets (pose, club, phase-classifier) plus their metadata manifests (PRD section 8.3: name, semver, checksum, input/output schema, supported platforms, evaluation report). Binary model files are gitignored by default until a Git LFS policy is set up (PRD section 11.1) — only justified, evaluated models should be committed. Not yet populated.
