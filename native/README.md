# native

Native iOS (Swift) and Android (Kotlin) modules for camera capture and pose/ML inference (PRD section 7.1). These bridge into the RN app via TurboModules/frame processors and must never pass full-resolution frames through the JS bridge (PRD section 7.3). Not yet implemented.
