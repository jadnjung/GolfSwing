source "https://rubygems.org"

ruby File.read(File.join(__dir__, ".ruby-version")).strip

gem "bundler", "~> 2.5"

# CocoaPods and Fastlane are intentionally not added yet — there is no
# ios/Podfile or Fastlane lane in this repository. They will be added in
# the app-scaffolding step once there is something for them to manage.
# See docs/adr/0002-toolchain-baseline.md.
